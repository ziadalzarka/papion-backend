# Gray Apps Mongoose Connector

Provides the `DATABASE_CONNECTION` set to mongoose connection.

Requires `ConfigUtils` class and needs these configurations to be set in the configuration JSON

```
- database
  - host
  - database
  - port
  - username
  - password
```